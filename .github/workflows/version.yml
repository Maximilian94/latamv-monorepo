name: Automated Versioning

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  version-check:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.PERSONAL_TOKEN }}
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Check conventional commits
      run: npx commitlint --from HEAD~1 --to HEAD

  version-bump:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main' && !contains(github.event.head_commit.message, '[skip ci]')
    
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.PERSONAL_TOKEN }}
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Configure Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
    
    - name: Configure Git to skip CI on automated commits
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        echo "Configured to skip CI on automated commits"
    
    - name: Install dependencies
      run: npm ci
    
    - name: Get current version
      id: current_version
      run: echo "version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
    
    - name: Bump version
      id: bump_version
      run: |
        npm run version:patch
        echo "new_version=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT
        # Add [skip ci] to prevent infinite loop
        git add .
        git commit -m "chore: bump version to $new_version [skip ci]"
    
    - name: Check if version changed
      id: version_changed
      run: |
        if [ "${{ steps.current_version.outputs.version }}" != "${{ steps.bump_version.outputs.new_version }}" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
        else
          echo "changed=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Push changes
      if: steps.version_changed.outputs.changed == 'true'
      run: |
        git push --follow-tags origin main
    
    - name: Check if tag exists
      id: check_tag
      run: |
        if git tag -l "v${{ steps.bump_version.outputs.new_version }}" | grep -q "v${{ steps.bump_version.outputs.new_version }}"; then
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "exists=false" >> $GITHUB_OUTPUT
        fi

    - name: Create Release
      if: steps.version_changed.outputs.changed == 'true' && steps.check_tag.outputs.exists == 'false'
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PERSONAL_TOKEN }}
      with:
        tag_name: v${{ steps.bump_version.outputs.new_version }}
        release_name: Release v${{ steps.bump_version.outputs.new_version }}
        body: |
          ## What's Changed
          
          This is an automated release. See the [CHANGELOG.md](./CHANGELOG.md) for detailed information about what changed in this version.
          
          ## Version Information
          - **Version:** ${{ steps.bump_version.outputs.new_version }}
          - **Build Date:** ${{ github.event.head_commit.timestamp }}
          - **Commit:** ${{ github.sha }}
        draft: false
        prerelease: false

    - name: Skip Release (Tag already exists)
      if: steps.version_changed.outputs.changed == 'true' && steps.check_tag.outputs.exists == 'true'
      run: |
        echo "Tag v${{ steps.bump_version.outputs.new_version }} already exists. Skipping release creation." 