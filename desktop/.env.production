# Baked into the production Tauri build (npm run tauri build).
# The installed app defaults to the Render backend; still overridable on the
# login screen.
VITE_API_BASE_URL=https://latamv-monorepo-be.onrender.com
